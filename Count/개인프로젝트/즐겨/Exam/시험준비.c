#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int ft_charset(char c)
{
    if ((c >= 9 && c <= 13 ) || c == ' ')
        return (1);
    return (0);
}

int total_length()
{
    
}
char    **ft_split(char *s)
{

}
